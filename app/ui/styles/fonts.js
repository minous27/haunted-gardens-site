import { Inter, Lusitana, Shadows_Into_Light } from "next/font/google";

export const inter = Inter({subsets: ['latin']});

export const shadinlit = Shadows_Into_Light({
    weight: ['400'],
    subsets: ['latin']
});

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin']
});